machine:
  python:
    version: 2.7.6

test:
  override:
    - make doctest

general:
  artifacts:
    - "build"

deployment:
  circle-ci-develop:
    branch: circle-ci/develop
    commands:
        - make html

  circle-ci-release:
    branch: circle-ci/release
    commands:
        - make html
